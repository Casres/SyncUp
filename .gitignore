# Dependencies
node_modules/

# Environment files
.env
.env.local
.env.*.local

# Build outputs
dist/
build/

# Expo / React Native
.expo/
social-calendar-mobile/ios/
social-calendar-mobile/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# Terraform — machine-generated provider cache + state (state holds secrets).
# The .terraform.lock.hcl IS committed (dependency lock, meant for VCS).
**/.terraform/
*.tfstate
*.tfstate.*

# Claude Code / Cowork — machine-specific local settings (allowlists, etc.)
.claude/settings.local.json
**/.claude/settings.local.json
